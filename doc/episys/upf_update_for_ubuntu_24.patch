diff --git a/docker/Dockerfile.upf.ubuntu b/docker/Dockerfile.upf.ubuntu
index ffd06f5..e63a221 100644
--- a/docker/Dockerfile.upf.ubuntu
+++ b/docker/Dockerfile.upf.ubuntu
@@ -23,14 +23,15 @@
 # Dockerfile for the Open-Air-Interface UPF service
 #   Valid for Ubuntu-20.04 (focal)
 #             Ubuntu-22.04 (jammy)
+#             Ubuntu-24.04 (noble)
 #
 #---------------------------------------------------------------------

 #---------------------------------------------------------------------
-# BASE IMAGE
+# BASE IMAGE - Auto-detect from build argument or use jammy as default
 #---------------------------------------------------------------------
-ARG BASE_IMAGE=ubuntu:jammy
-FROM $BASE_IMAGE as oai-upf-base
+ARG UBUNTU_VERSION
+FROM ubuntu:${UBUNTU_VERSION:-jammy} AS oai-upf-base

 ENV DEBIAN_FRONTEND=noninteractive
 ENV TZ=Europe/Paris
@@ -41,8 +42,37 @@ RUN apt-get update && \
     DEBIAN_FRONTEND=noninteractive apt-get install --yes \
       psmisc \
       git \
+      build-essential \
+      cmake \
+      pkg-config \
+      libssl-dev \
+      libunwind-dev \
+      libconfig++-dev \
   && rm -rf /var/lib/apt/lists/*

+# Install version-specific dependencies based on Ubuntu release
+RUN . /etc/os-release && \
+    apt-get update && \
+    if [ "$VERSION_CODENAME" = "jammy" ]; then \
+      DEBIAN_FRONTEND=noninteractive apt-get install -y \
+        libspdlog-dev \
+        libyaml-cpp-dev \
+        libgoogle-glog-dev \
+        libboost-all-dev; \
+    elif [ "$VERSION_CODENAME" = "noble" ]; then \
+      DEBIAN_FRONTEND=noninteractive apt-get install -y \
+        libspdlog-dev \
+        libyaml-cpp-dev \
+        libgoogle-glog-dev \
+        libboost-all-dev \
+        libstdc++-13-dev; \
+    fi && \
+    rm -rf /var/lib/apt/lists/*
+
+# Fix: Define paths explicitly to avoid discovery issues
+ENV PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:/usr/local/lib64/pkgconfig:/usr/lib/pkgconfig
+ENV CMAKE_PREFIX_PATH=/usr/local
+
 # Some GIT configuration commands quite useful
 RUN git config --global https.postBuffer 123289600 && \
     git config --global http.sslverify false
@@ -54,6 +84,40 @@ COPY ./build/common-build /openair-upf/build/common-build
 COPY ./build/upf/CMakeLists.txt /openair-upf/build/upf/CMakeLists.txt
 COPY ./build/cmake_modules /openair-upf/cmake_modules/

+# CMAKE SHIM: forces CPR discovery for configure calls only
+RUN mv /usr/bin/cmake /usr/bin/cmake.real && \
+    { \
+      echo '#!/bin/bash'; \
+      echo 'case "$1" in'; \
+      echo '  -E|--build|--install|--version|--help)'; \
+      echo '    exec /usr/bin/cmake.real "$@"'; \
+      echo '    ;;'; \
+      echo '  *)'; \
+      echo '    exec /usr/bin/cmake.real -DCMAKE_PREFIX_PATH=/usr/local -Dcpr_DIR=/usr/local/lib/cmake/cpr "$@"'; \
+      echo '    ;;'; \
+      echo 'esac'; \
+    } > /usr/bin/cmake && \
+    chmod +x /usr/bin/cmake
+
+# SPOOF OS & FIX SYMLINK CRASH for Ubuntu 24.04
+RUN . /etc/os-release && \
+    if [ "$VERSION_CODENAME" = "noble" ]; then \
+      sed -i 's/noble/jammy/g' /etc/os-release && \
+      sed -i 's/24.04/22.04/g' /etc/os-release; \
+    fi && \
+    rm -f /usr/include/sys/*.h
+
+# Apply build fixes for Ubuntu 24.04 (GCC 13+ compatibility)
+# Fix CPR: disable -Werror for deprecated curl APIs
+RUN sed -i \
+      's|cmake .. -DCPR_USE_SYSTEM_CURL=ON -DCPR_BUILD_TESTS=OFF|cmake .. -DCPR_USE_SYSTEM_CURL=ON -DCPR_BUILD_TESTS=OFF -DCMAKE_CXX_FLAGS="-Wno-error=deprecated-declarations"|' \
+      /openair-upf/build/common-build/installation/build_helper.cpr
+
+# Fix Folly: add missing #include <system_error> for GCC 13+
+RUN sed -i \
+      '/s@folly::constexpr_max@@/a\    sed -i "1i #include <system_error>" folly/detail/AtFork.cpp' \
+      /openair-upf/build/common-build/installation/build_helper.fb_folly
+
 # Installing all the needed libraries/packages to build and run UPF
 WORKDIR /openair-upf/build/scripts
 RUN ./build_upf --install-deps --force
@@ -61,9 +125,30 @@ RUN ./build_upf --install-deps --force
 #---------------------------------------------------------------------
 # BUILDER IMAGE
 #---------------------------------------------------------------------
-FROM oai-upf-base as oai-upf-builder
+FROM oai-upf-base AS oai-upf-builder
+
+# Inheritance of environment
+ENV PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:/usr/local/lib64/pkgconfig:/usr/lib/pkgconfig
+ENV CMAKE_PREFIX_PATH=/usr/local
+
 # Copy the rest of source code
 COPY . /openair-upf
+
+# Re-apply CMAKE SHIM (COPY overwrites /usr/bin/cmake from BASE)
+RUN if [ ! -f /usr/bin/cmake.real ]; then mv /usr/bin/cmake /usr/bin/cmake.real; fi && \
+    { \
+      echo '#!/bin/bash'; \
+      echo 'case "$1" in'; \
+      echo '  -E|--build|--install|--version|--help)'; \
+      echo '    exec /usr/bin/cmake.real "$@"'; \
+      echo '    ;;'; \
+      echo '  *)'; \
+      echo '    exec /usr/bin/cmake.real -DCMAKE_PREFIX_PATH=/usr/local -Dcpr_DIR=/usr/local/lib/cmake/cpr "$@"'; \
+      echo '    ;;'; \
+      echo 'esac'; \
+    } > /usr/bin/cmake && \
+    chmod +x /usr/bin/cmake
+
 # Building UPF
 WORKDIR /openair-upf/build/scripts
 RUN  if [ "$ENABLE_LTTNG" = "true" ]; then apt-get update && \
@@ -82,10 +167,25 @@ RUN  if [ "$ENABLE_LTTNG" = "true" ]; then apt-get update && \
     mv /openair-upf/build/upf/build/upf /openair-upf/build/upf/build/oai_upf && \
     echo "end of UPF build"

+# Install Iperf3 and ffmpeg for testing
+RUN apt-get update && \
+    apt-get install -y build-essential git autoconf automake libtool ffmpeg && \
+    rm -rf /var/lib/apt/lists/*
+
+WORKDIR /app
+RUN git clone https://github.com/esnet/iperf.git && \
+    cd iperf && \
+    git checkout 3.16 && \
+    ./bootstrap.sh && \
+    ./configure && \
+    make && \
+    make install
+
 #---------------------------------------------------------------------
 # TARGET IMAGE
 #---------------------------------------------------------------------
-FROM $BASE_IMAGE as oai-upf
+ARG UBUNTU_VERSION
+FROM ubuntu:${UBUNTU_VERSION:-jammy} AS oai-upf
 ENV DEBIAN_FRONTEND=noninteractive
 ENV TZ=Europe/Paris
 ARG ENABLE_LTTNG=false
@@ -99,33 +199,52 @@ RUN apt-get update && \
       iproute2 \
       ethtool \
       arping \
-      libllvm14 \
       libnl-3-200 \
       libnl-route-3-200 \
       sudo \
       iptables \
       netbase \
-# Ubuntu 20 --> libasan5
-# Ubuntu 22 --> libasan6
-#      libasan? \
       libgssapi-krb5-2 \
-# Ubuntu 20 --> libldap-2.4-2
-# Ubuntu 22 --> libldap-2.5-0
       libldap-2.?-? \
-      libgoogle-glog0v5 \
+      libldap-dev \
       libdouble-conversion3 \
       libconfig++9v5 \
       librtmp1 \
       libpsl5 \
       libnghttp2-14 \
       libcurl?-gnutls \
-# Ubuntu 20 --> boost71
-# Ubuntu 22 --> boost74
-      libboost-thread1.7?.0 \
-# lttng
-  && if [ "$ENABLE_LTTNG" = "true" ]; then DEBIAN_FRONTEND=noninteractive apt-get install --yes liblttng-ust-dev; fi \
   && rm -rf /var/lib/apt/lists/*

+# Install version-specific runtime dependencies
+RUN . /etc/os-release && \
+    apt-get update && \
+    if [ "$VERSION_CODENAME" = "jammy" ]; then \
+      DEBIAN_FRONTEND=noninteractive apt-get install -y \
+        libllvm14 \
+        libgoogle-glog0v5 \
+        libboost-thread1.74.0; \
+    elif [ "$VERSION_CODENAME" = "noble" ]; then \
+      DEBIAN_FRONTEND=noninteractive apt-get install -y \
+        libllvm18 \
+        libasan8 \
+        libgoogle-glog0v6 \
+        libboost-thread1.83.0; \
+    fi && \
+    if [ "$ENABLE_LTTNG" = "true" ]; then \
+      DEBIAN_FRONTEND=noninteractive apt-get install --yes liblttng-ust-dev; \
+    fi && \
+    rm -rf /var/lib/apt/lists/*
+
+# Install testing tools in runtime image
+RUN apt-get update && \
+    apt-get install -y ffmpeg && \
+    rm -rf /var/lib/apt/lists/*
+
+# Copy iperf3 binary and shared library from BUILDER
+COPY --from=oai-upf-builder /usr/local/bin/iperf3 /usr/local/bin/
+COPY --from=oai-upf-builder /usr/local/lib/libiperf.so* /usr/local/lib/
+RUN ldconfig
+
 # Copying executable and generated libraries
 WORKDIR /openair-upf/bin
 COPY --from=oai-upf-builder \
diff --git a/src/oai_upf/CMakeLists.txt b/src/oai_upf/CMakeLists.txt
index 3f25430..4e50c44 100644
--- a/src/oai_upf/CMakeLists.txt
+++ b/src/oai_upf/CMakeLists.txt
@@ -171,10 +171,10 @@ set(CMAKE_C_FLAGS_RELWITHDEBINFO "${CMAKE_C_FLAGS} -g  -O1 ")
 set(CMAKE_C_FLAGS_MINSIZEREL "${CMAKE_C_FLAGS} -Os -s")

 # CMAKE_CXX_FLAGS_DEBUG: -fsanitize=address not compatible with valgrind
-set(CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS} -std=c++17  -g -fstack-protector-all -DMALLOC_CHECK_=3 -O0 -fno-omit-frame-pointer")
-set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS} -std=c++17  -O2 -fno-omit-frame-pointer -s ")
-set(CMAKE_CXX_FLAGS_RELWITHDEBINFO "${CMAKE_CXX_FLAGS} -std=c++17 -g -O1 ")
-set(CMAKE_CXX_FLAGS_MINSIZEREL "${CMAKE_CXX_FLAGS} -std=c++17 -Os -s")
+set(CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS} -std=c++17 -include cstdint -g -fstack-protector-all -DMALLOC_CHECK_=3 -O0 -fno-omit-frame-pointer")
+set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS} -std=c++17 -include cstdint -O2 -fno-omit-frame-pointer -s ")
+set(CMAKE_CXX_FLAGS_RELWITHDEBINFO "${CMAKE_CXX_FLAGS} -std=c++17 -include cstdint -g -O1 ")
+set(CMAKE_CXX_FLAGS_MINSIZEREL "${CMAKE_CXX_FLAGS} -std=c++17 -include cstdint -Os -s")


 ################################################################
diff --git a/src/upf_app/app/upf_config.hpp b/src/upf_app/app/upf_config.hpp
index 3ee7192..f2b0785 100644
--- a/src/upf_app/app/upf_config.hpp
+++ b/src/upf_app/app/upf_config.hpp
@@ -152,7 +152,7 @@ class upf_config {
         pdns(),
         smfs(),
         max_pfcp_sessions(100),
-        nsf(true),
+        nsf({true}),
         enable_snat(false),
         enable_fr(false),
         nrf_addr() {
diff --git a/src/upf_app/simpleswitch/pfcp_pdr.cpp b/src/upf_app/simpleswitch/pfcp_pdr.cpp
index 402f16b..759deac 100644
--- a/src/upf_app/simpleswitch/pfcp_pdr.cpp
+++ b/src/upf_app/simpleswitch/pfcp_pdr.cpp
@@ -105,10 +105,17 @@ bool pfcp_pdr::look_up_pack_in_core(
       return false;
     }

-    in_addr _addr4{};
-    unsigned char buf_in_addr[sizeof(struct in_addr)];
-    if (inet_pton(AF_INET, "10.0.0.2", buf_in_addr) == 1) {
-      memcpy(&_addr4, buf_in_addr, sizeof(struct in_addr));
+    in_addr _addr4 = {iph->daddr};
+    uint32_t dest_addr = ntohl(iph->daddr);
+    uint8_t last_octet = dest_addr & 0xFF;
+    Logger::upf_n4().info("NOTE__: Rx dest_byte4 = %u (if it >= 100, it will changed to %s)\n", last_octet, connected_ue_ip.c_str());
+    if (last_octet >= 100) {
+      printf("NOTE__: Changed  The last octet is >= 100: %u to %s\n", last_octet, connected_ue_ip.c_str());
+      Logger::upf_n4().info("NOTE__: Changed  The last octet is >= 100: %u to %s\n", last_octet, connected_ue_ip.c_str());
+      unsigned char buf_in_addr[sizeof(struct in_addr)];
+      if (inet_pton(AF_INET, connected_ue_ip.c_str(), buf_in_addr) == 1) {
+        memcpy(&_addr4, buf_in_addr, sizeof(struct in_addr));
+      }
     }

     if (!upf_cfg.enable_fr &&
diff --git a/src/upf_app/simpleswitch/pfcp_pdr.hpp b/src/upf_app/simpleswitch/pfcp_pdr.hpp
index 03997b4..ff84abe 100644
--- a/src/upf_app/simpleswitch/pfcp_pdr.hpp
+++ b/src/upf_app/simpleswitch/pfcp_pdr.hpp
@@ -52,6 +52,7 @@ class pfcp_pdr {
   std::pair<bool, pfcp::qer_id_t> qer_id;
   std::pair<bool, pfcp::activate_predefined_rules_t> activate_predefined_rules;
   bool notified_cp;
+  std::string connected_ue_ip;

   explicit pfcp_pdr(uint64_t lseid)
       : lock(),
diff --git a/src/upf_app/simpleswitch/pfcp_switch.cpp b/src/upf_app/simpleswitch/pfcp_switch.cpp
index 7cb85f4..dd14abd 100644
--- a/src/upf_app/simpleswitch/pfcp_switch.cpp
+++ b/src/upf_app/simpleswitch/pfcp_switch.cpp
@@ -897,6 +897,22 @@ void pfcp_switch::handle_pfcp_session_establishment_request(
               << std::endl;
     for (const auto& it : up_seid2pfcp_sessions) {
       std::cout << it.second->to_string() << std::endl;
+      for (std::vector<std::shared_ptr<pfcp::pfcp_pdr>>::const_iterator it_pdr =
+              it.second->pdrs.begin();
+          it_pdr != it.second->pdrs.end(); ++it_pdr) {
+        std::shared_ptr<pfcp::pfcp_pdr> pdr = *it_pdr;
+
+        if (pdr->pdi.first) {
+          if (pdr->pdi.second.ue_ip_address.first) {
+            std::string ip = {};
+            if (pdr->pdi.second.ue_ip_address.second.v4) {
+              ip = oai::utils::conv::toString(
+                  pdr->pdi.second.ue_ip_address.second.ipv4_address);
+              connected_ue_ip = ip;
+            }
+          }
+        }
+      }
     }
   }
 }
@@ -1358,12 +1374,16 @@ void pfcp_switch::pfcp_session_look_up_pack_in_core(
   if (iph->version == 4) {
     uint32_t ue_ip = be32toh(iph->daddr);
     // Set UE IP addr to the adress of the Relay
-    in_addr _addr4{};
-    unsigned char buf_in_addr[sizeof(struct in_addr)];
-    if (inet_pton(AF_INET, "10.0.0.2", buf_in_addr) == 1) {
-      memcpy(&_addr4, buf_in_addr, sizeof(struct in_addr));
+    uint8_t last_octet = ue_ip & 0xFF;
+    Logger::pfcp_switch().info("NOTE__: The last octet is >= 100 ? : %u\n", last_octet);
+    if (last_octet >= 100) {
+      in_addr _addr4{};
+      unsigned char buf_in_addr[sizeof(struct in_addr)];
+      if (inet_pton(AF_INET, connected_ue_ip.c_str(), buf_in_addr) == 1) {
+        memcpy(&_addr4, buf_in_addr, sizeof(struct in_addr));
+      }
+      ue_ip = ntohl(_addr4.s_addr);
     }
-    ue_ip = ntohl(_addr4.s_addr);

     bool is_pdr_ue_ip = get_pfcp_dl_pdrs_by_ue_ip(ue_ip, pdrs);
     if (!is_pdr_ue_ip && upf_cfg.enable_fr) {
@@ -1377,6 +1397,7 @@ void pfcp_switch::pfcp_session_look_up_pack_in_core(
       for (std::vector<std::shared_ptr<pfcp::pfcp_pdr>>::iterator it =
                pdrs->begin();
            it < pdrs->end(); ++it) {
+        (*it)->connected_ue_ip = connected_ue_ip;
         if ((*it)->look_up_pack_in_core(iph, num_bytes)) {
           std::shared_ptr<pfcp::pfcp_session> ssession = {};
           uint64_t lseid                               = 0;
diff --git a/src/upf_app/simpleswitch/pfcp_switch.hpp b/src/upf_app/simpleswitch/pfcp_switch.hpp
index adbb9b1..8432b62 100644
--- a/src/upf_app/simpleswitch/pfcp_switch.hpp
+++ b/src/upf_app/simpleswitch/pfcp_switch.hpp
@@ -74,6 +74,7 @@ class pfcp_switch {
   folly::MPMCQueue<iovec_q_item_t*>* free_pool_;
   folly::MPMCQueue<iovec_q_item_t*>* work_pool_;
   uint32_t num_threads_;
+  std::string connected_ue_ip;
   char* recv_buffer_alloc_;
   char recv_buffer_[PFCP_SWITCH_RECV_BUFFER_SIZE];
   std::thread pwThread_;
